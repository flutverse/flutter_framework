# Assets

Application assets are grouped by purpose:

- `images`: raster and vector image assets.
- `icons`: custom icons and icon resources.
- `fonts`: local font files.
- `json`: static configuration and sample data.

Register assets in `pubspec.yaml` when they are used by the Flutter application.
